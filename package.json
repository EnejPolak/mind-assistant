{
  "name": "mind-assistant",
  "version": "1.0.0",
  "description": "Digital assistant for mental health - WordPress theme",
  "scripts": {
    "dev": "php -S localhost:8000 & browser-sync start --proxy 'localhost:8000' --files '**/*.php, **/*.css, **/*.js' --no-notify",
    "start": "php -S localhost:8000"
  },
  "keywords": [],
  "author": "Enej Polak",
  "license": "GPL-2.0-or-later",
  "devDependencies": {
    "browser-sync": "^3.0.2"
  }
}

